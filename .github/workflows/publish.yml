name: 🚀 AGRO_TECH Smart Publish

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# --- صلاحيات الوصول (مهمة جداً لرفع الصورة لـ GitHub Packages) ---
permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      # 1. تسجيل الدخول إلى سجل حاويات GitHub
      - name: 🔑 Log in to GitHub Packages
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 2. إعداد Docker Buildx (لتحسين الكاش والبناء المتعدد)
      - name: 🛠️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. بناء ورفع الصورة مع حقن الـ 24 متغيراً
      - name: 🏗️ Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/agro_web_app:latest
          # تفعيل الكاش لتسريع البناء مستقبلاً
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # حقن كافة المتغيرات الموحدة من Secrets
          build-args: |
            DJANGO_SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }}
            DATABASE_URL=${{ secrets.DATABASE_URL }}
            SH_CLIENT_ID=${{ secrets.SH_CLIENT_ID }}
            SH_CLIENT_SECRET=${{ secrets.SH_CLIENT_SECRET }}
            SH_INSTANCE_ID=${{ secrets.SH_INSTANCE_ID }}
            GOOGLE_MAPS_API_KEY=${{ secrets.GOOGLE_MAPS_API_KEY }}
            FAO_API_KEY=${{ secrets.FAO_API_KEY }}
            WAPOR_API_KEY=${{ secrets.WAPOR_API_KEY }}
            HF_TOKEN=${{ secrets.HF_TOKEN }}
            LANCEDB_URI=${{ secrets.LANCEDB_URI }}
            MODAL_TOKEN_ID=${{ secrets.MODAL_TOKEN_ID }}
            MODAL_TOKEN_SECRET=${{ secrets.MODAL_TOKEN_SECRET }}
            R2_ACCESS_KEY=${{ secrets.R2_ACCESS_KEY }}
            R2_SECRET_KEY=${{ secrets.R2_SECRET_KEY }}
            R2_BUCKET_NAME=${{ secrets.R2_BUCKET_NAME }}
            R2_ENDPOINT=${{ secrets.R2_ENDPOINT }}
            REDIS_URL=${{ secrets.REDIS_URL }}
            REDIS_TOKEN=${{ secrets.REDIS_TOKEN }}
            FIREBASE_CONFIG_PATH=${{ secrets.FIREBASE_CONFIG_PATH }}
            FIREBASE_MESSAGING_ID=${{ secrets.FIREBASE_MESSAGING_ID }}
            MAILERSEND_API_KEY=${{ secrets.MAILERSEND_API_KEY }}
            MAILERSEND_USERNAME=${{ secrets.MAILERSEND_USERNAME }}
            DEFAULT_FROM_EMAIL=${{ secrets.DEFAULT_FROM_EMAIL }}
            JITSI_DOMAIN=${{ secrets.JITSI_DOMAIN }}
            JITSI_APP_ID=${{ secrets.JITSI_APP_ID }}

      - name: ✅ Verify Build
        run: echo "Successfully published AGRO_TECH image to GHCR!"